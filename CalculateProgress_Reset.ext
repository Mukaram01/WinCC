void CalculateProgress_Reset(void) {
    /* Reset the monotonic floor so the next job's percentage starts from zero.
       s_progHighWater is a module-level global defined in CalculateProgress.c;
       declared extern here so this separate action file can write to it. */
    extern double s_progHighWater;
    s_progHighWater = 0.0;
}
