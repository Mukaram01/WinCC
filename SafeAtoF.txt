#include "apdefap.h"

double SafeAtoF(const char* str) {
    char* endptr = 0;
    double value = 0.0;

    if (!str || *str == '\0')
        return 0.0;

    value = strtod(str, &endptr);
    if (endptr == str)
        return 0.0;

    return value;
}